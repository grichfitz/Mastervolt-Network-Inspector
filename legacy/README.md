# Legacy Archive

This folder stores historical/obsolete artifacts kept for reference during the transition from prototype to production-style structure.

Archived now:

- `legacy/backend-src/time.js` (unused date utility from early parser experiments)
- `legacy/untitled.env.snippet` (scratch file captured before cleanup)

Notes:

- Active ingestion code now lives in `backend/ingest/`.
- Legacy compatibility wrappers remain in `backend/src/parsers` and `backend/src/services`.
